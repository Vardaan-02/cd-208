export default function Main({ setName }) {

    return (
        <div className="content w-full h-full border-4">
            <h1>Zustand File Explorer</h1>
            <p>Teach global state management using a read world tree file system</p>
        </div>
    );
}